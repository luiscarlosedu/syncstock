import { AuthHeader } from "../../../components/auth-header";
import {
    Container
} from "./styles";

export default function RegisterEmployee() {
    return (
        <Container>
            <AuthHeader />
        </Container>
    );
}