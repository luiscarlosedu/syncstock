import { Overlay } from "./styles";

// interface UpdateModalProps {
//     isOpen: boolean;
//     onClose: () => void;
// }

export function UpdateProductModal(/*{
    isOpen, onClose
}: UpdateModalProps*/) {
    return (
        <Overlay>
            
        </Overlay>
    );
}